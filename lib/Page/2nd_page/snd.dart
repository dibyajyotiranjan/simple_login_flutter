
import 'package:flutter/material.dart';

class snd extends StatelessWidget {
  const snd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Center(
        child: Text("Currency Page",style: TextStyle(color: Colors.yellow),),
      ),
    );
  }
}
