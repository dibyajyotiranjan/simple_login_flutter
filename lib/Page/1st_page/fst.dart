
import 'package:flutter/material.dart';

class Fstpage extends StatelessWidget {
  const Fstpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Center(
        child: Text("All details of page"),
      ),
    );
  }
}
