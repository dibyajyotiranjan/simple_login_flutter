
import 'package:flutter/material.dart';

class fth extends StatelessWidget {
  const fth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      child: Center(
        child: Text("Setting of the application"),
      ),
    );
  }
}
