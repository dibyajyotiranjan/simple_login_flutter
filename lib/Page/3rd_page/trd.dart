
import 'package:flutter/material.dart';

class Trdpage extends StatelessWidget {
  const Trdpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purpleAccent,
      child: Center(
        child: Text("Account Lock page"),
      ),
    );
  }
}
